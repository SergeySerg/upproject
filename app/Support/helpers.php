<?php

if (! function_exists('getSetting')) {
    /**
     * Format text.
     *
     * @param  string  $text
     * @return string
     */
    function getSetting($text)
    {
        // Format text
        return $text;
    }
}